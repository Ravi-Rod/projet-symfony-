{% extends 'base.html.twig' %}

{% block title %}Entree{% endblock %}

{% block body %}
    <h1>Entree</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entree.id }}</td>
            </tr>
            <tr>
                <th>Quantite</th>
                <td>{{ entree.quantite }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ entree.prix }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ entree.date ? entree.date|date('Y-m-d') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <div class="mt-4 mb-0"><a href="{{ path('app_entree_index') }}">back to list</a></div>

    <div class="btn"><a href="{{ path('app_entree_edit', {'id': entree.id}) }}">edit</a></div>

    {{ include('entree/_delete_form.html.twig') }}
{% endblock %}
